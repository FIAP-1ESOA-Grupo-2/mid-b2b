import { Metadata } from "next";

export const metadata = {
    title: 'Dashboard',
} satisfies Metadata

const DashboardPage = () => {
    return (
        <main>
            Pagina inicial
        </main>
    )
}

export default DashboardPage